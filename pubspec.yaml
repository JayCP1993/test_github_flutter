name: my_flutter_labs
description: A new Flutter project for labs and experiments.
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^8.0.0
  url_launcher: ^6.2.4
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# To add assets such as images
flutter:
  uses-material-design: true

  assets:
    - images/question-mark.jpg
    - images/light-bulb.jpg
    - images/stop-sign.jpg
